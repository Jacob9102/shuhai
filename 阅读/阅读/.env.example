# 复制为 .env 后按需修改（docker compose 会自动读取）

# 宿主机映射端口
SHUHAI_PUBLISH_PORT=8080

# 时区
TZ=Asia/Shanghai

# 并发搜索的书源数量（8~24 之间比较稳妥）
SHUHAI_SEARCH_CONCURRENCY=16

# 单书源请求超时（毫秒）
SHUHAI_TIMEOUT=15000

# 同站点最小请求间隔（毫秒），调大可降低被封风险
SHUHAI_HOST_INTERVAL_MS=120

# 书源内嵌 JS 执行超时（毫秒）
SHUHAI_JS_TIMEOUT=3000
